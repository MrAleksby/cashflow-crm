rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Разрешить все операции без авторизации (для разработки)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

